// #ifndef IMAGE_PROCESSING
// #define IMAGE_PROCESSING
// #include <stdbool.h>

// typedef struct {
//     size_t width;
//     size_t height;
//     size_t channels;
//     unsigned char* data;
// } image_t;

// image_t load_image_as_grayscale(const char* file_path);

// image_t make_resized_grayscale(image_t* original, size_t max_width, size_t max_height);

// void print_image(image_t* image, bool dark_mode);
// #endif